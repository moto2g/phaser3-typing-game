import Main from './main-scene';
import Loading from './loading-scene';
import Title from './title-scene';
import BeforeTitle from './before-title-scene';

export default [Loading, Main, Title, BeforeTitle];
export { Main, Loading, Title, BeforeTitle };